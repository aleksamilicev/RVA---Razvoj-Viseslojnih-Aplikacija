<UserControl x:Class="RVA.Client.Views.LocationListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RVA.Client.Views"
             xmlns:conv="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Location Management" 
                   FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="20,10"/>

        <!-- Toolbar -->
        <Border Grid.Row="1" Background="#F0F0F0" BorderBrush="#CCCCCC" BorderThickness="0,1">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Filters Row -->
                <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                             VerticalAlignment="Center" Margin="0,0,15,0"/>

                    <TextBlock Text="Filter by Parking:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox Width="120" SelectedItem="{Binding FilterHasParking}" 
                              VerticalAlignment="Center" Margin="0,0,15,0">
                        <ComboBox.Items>
                            <ComboBoxItem Content="All" Tag="{x:Null}"/>
                            <ComboBoxItem Content="Has Parking" Tag="True"/>
                            <ComboBoxItem Content="No Parking" Tag="False"/>
                        </ComboBox.Items>
                    </ComboBox>

                    <Button Content="Clear Filters" Command="{Binding ClearFiltersCommand}" 
                            Margin="5,0" Padding="10,5"/>
                </StackPanel>

                <!-- Action Buttons Row -->
                <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal">
                    <Button Content="Add New" Command="{Binding AddNewCommand}" 
                            Background="#4CAF50" Foreground="White" 
                            Margin="0,0,10,0" Padding="15,8" FontWeight="Bold"/>

                    <Button Content="Edit" Command="{Binding EditCommand}" 
                            Background="#2196F3" Foreground="White" 
                            Margin="0,0,10,0" Padding="15,8"/>

                    <Button Content="Delete" Command="{Binding DeleteCommand}" 
                            Background="#F44336" Foreground="White" 
                            Margin="0,0,10,0" Padding="15,8"/>

                    <Button Content="View Details" Command="{Binding ViewDetailsCommand}" 
                            Background="#FF9800" Foreground="White" 
                            Margin="0,0,10,0" Padding="15,8"/>
                </StackPanel>

                <!-- Refresh Button -->
                <Button Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" 
                        Content="🔄 Refresh" Command="{Binding RefreshCommand}" 
                        VerticalAlignment="Center" Padding="10,8"/>
            </Grid>
        </Border>

        <!-- Main Content - DataGrid -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding LocationsView}"
                  SelectedItem="{Binding SelectedLocation}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  SelectionMode="Single"
                  AlternatingRowBackground="#F9F9F9"
                  Margin="10">

            <DataGrid.Columns>
                <!-- Name -->
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150" CanUserSort="True"/>

                <!-- River -->
                <DataGridTextColumn Header="River" Binding="{Binding River}" Width="120" CanUserSort="True"/>

                <!-- Coordinates -->
                <DataGridTemplateColumn Header="Coordinates" Width="140" CanUserSort="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <Run Text="{Binding Latitude, StringFormat='F4'}"/>
                                <Run Text=","/>
                                <Run Text="{Binding Longitude, StringFormat='F4'}"/>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Description -->
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" 
                                    Width="200" CanUserSort="False">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="MaxWidth" Value="200"/>
                            <Setter Property="ToolTip" Value="{Binding Description}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Has Parking -->
                <DataGridTemplateColumn Header="Parking" Width="70" CanUserSort="True" SortMemberPath="HasParking">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding HasParking, Mode=OneWay}" IsEnabled="False" 
                                      HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Has Facilities -->
                <DataGridTemplateColumn Header="Facilities" Width="80" CanUserSort="True" SortMemberPath="HasFacilities">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding HasFacilities, Mode=OneWay}" IsEnabled="False" 
                                      HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Created Date -->
                <DataGridTextColumn Header="Created" Binding="{Binding CreatedDate, StringFormat='dd/MM/yyyy'}" 
                                    Width="90" CanUserSort="True"/>
            </DataGrid.Columns>

            <!-- Context Menu -->
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Edit" Command="{Binding EditCommand}"/>
                    <MenuItem Header="View Details" Command="{Binding ViewDetailsCommand}"/>
                    <Separator/>
                    <MenuItem Header="Delete" Command="{Binding DeleteCommand}"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="#E0E0E0" BorderBrush="#CCCCCC" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" 
                           VerticalAlignment="Center" Margin="10,5"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,5">
                    <ProgressBar Width="100" Height="15" IsIndeterminate="{Binding IsLoading}" 
                            Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"/>
                    <TextBlock Text="Loading..." Margin="5,0" 
                            Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>