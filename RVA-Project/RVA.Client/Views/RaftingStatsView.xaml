<UserControl x:Class="RVA.Client.Views.RaftingStatsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Unloaded="UserControl_Unloaded">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Rafting Statistics Dashboard" 
                   FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"/>

        <!-- Control Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,0,0,10">
            <Button Content="Start Monitoring" 
                    Command="{Binding StartMonitoringCommand}"
                    Margin="0,0,5,0" Padding="10,5"/>
            <Button Content="Stop Monitoring" 
                    Command="{Binding StopMonitoringCommand}"
                    Margin="5,0,5,0" Padding="10,5"/>
            <Button Content="Refresh" 
                    Command="{Binding RefreshCommand}"
                    Margin="5,0,0,0" Padding="10,5"/>
        </StackPanel>

        <!-- Statistics Cards -->
        <UniformGrid Grid.Row="2" Rows="1" Columns="5" Margin="0,0,0,20">
            <Border Background="#E3F2FD" CornerRadius="8" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Planned" FontWeight="Bold" 
                               HorizontalAlignment="Center" Foreground="#1976D2"/>
                    <TextBlock Text="{Binding PlannedCount}" FontSize="28" 
                               FontWeight="Bold" HorizontalAlignment="Center" 
                               Foreground="#1976D2"/>
                </StackPanel>
            </Border>

            <Border Background="#FFF3E0" CornerRadius="8" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Boarding" FontWeight="Bold" 
                               HorizontalAlignment="Center" Foreground="#F57C00"/>
                    <TextBlock Text="{Binding BoardingCount}" FontSize="28" 
                               FontWeight="Bold" HorizontalAlignment="Center" 
                               Foreground="#F57C00"/>
                </StackPanel>
            </Border>

            <Border Background="#E8F5E8" CornerRadius="8" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Paddling" FontWeight="Bold" 
                               HorizontalAlignment="Center" Foreground="#388E3C"/>
                    <TextBlock Text="{Binding PaddlingCount}" FontSize="28" 
                               FontWeight="Bold" HorizontalAlignment="Center" 
                               Foreground="#388E3C"/>
                </StackPanel>
            </Border>

            <Border Background="#F3E5F5" CornerRadius="8" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Resting" FontWeight="Bold" 
                               HorizontalAlignment="Center" Foreground="#7B1FA2"/>
                    <TextBlock Text="{Binding RestingCount}" FontSize="28" 
                               FontWeight="Bold" HorizontalAlignment="Center" 
                               Foreground="#7B1FA2"/>
                </StackPanel>
            </Border>

            <Border Background="#FFEBEE" CornerRadius="8" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Finished" FontWeight="Bold" 
                               HorizontalAlignment="Center" Foreground="#D32F2F"/>
                    <TextBlock Text="{Binding FinishedCount}" FontSize="28" 
                               FontWeight="Bold" HorizontalAlignment="Center" 
                               Foreground="#D32F2F"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Chart with Canvas -->
        <GroupBox Grid.Row="3" Header="Real-time Rafting States Chart" 
                  FontWeight="Bold" Margin="0,0,0,10">
            <Grid>
                <Canvas x:Name="ChartCanvas" 
                        Background="White"
                        Height="300"
                        MinHeight="300" />

                <!-- Loading Overlay -->
                <Border Background="#80FFFFFF" 
                        Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                        CornerRadius="5">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" Width="100" Height="20" Margin="0,0,0,10"/>
                        <TextBlock Text="Loading..." HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </GroupBox>

        <!-- Status and History -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <!-- Status -->
            <GroupBox Grid.Column="0" Header="Status" FontWeight="Bold" Margin="0,0,5,0">
                <TextBlock Text="{Binding StatusMessage}" 
                           VerticalAlignment="Center" 
                           TextWrapping="Wrap"
                           Padding="10"/>
            </GroupBox>

            <!-- History Log -->
            <GroupBox Grid.Column="1" Header="History Log" FontWeight="Bold" Margin="5,0,0,0">
                <ListBox ItemsSource="{Binding HistoryLog}" 
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         FontFamily="Consolas" FontSize="11">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="2"/>
                            <Setter Property="Margin" Value="0"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </GroupBox>
        </Grid>

    </Grid>
</UserControl>
